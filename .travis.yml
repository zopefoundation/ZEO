language: python
sudo: false
matrix:
    include:
        - os: linux
          python: 3.4
        - os: linux
          python: 3.5
install:
    - pip install -U setuptools
    - python bootstrap.py
    - bin/buildout
script:
    - bin/test -v1j99
notifications:
    email: false
